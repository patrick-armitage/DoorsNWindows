%li
  .row-fluid
    .span4 Username: #{ link_to user.name, user }
    .span4 Email: #{ user.email }
    - if current_user.admin? && !current_user?(user)
      #delete.span1.offset3= link_to "&times;".html_safe, user, method: :delete, data: { confirm: "Delete User?" }, class: 'close'
%li.divider